import { Navbar } from "../../components/Navbar"
import { Options } from "../../components/SignUp/Options"

export const SignUp = () => {
    return (
        <main>
            <Navbar />
            <Options />
        </main>
    )
}